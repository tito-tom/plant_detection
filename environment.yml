name: yolo_seg_root
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0.0
      - torchvision>=0.15.0
      - ultralytics>=8.3.0
      - albumentations>=1.3.0
      - optuna>=3.0.0
      - optuna-dashboard>=0.15.0
      - opencv-python>=4.8.0
      - matplotlib>=3.7.0
      - tqdm>=4.65.0
      - pyyaml>=6.0
      - scipy>=1.10.0

